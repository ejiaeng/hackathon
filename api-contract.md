# API Contract

## Overview
This document outlines the API endpoints shared between the Frontend and Backend.

## Endpoints

### Example Endpoint
- **GET** `/api/hello`
- **Description**: Returns a greeting.
- **Response**:
  ```json
  {
    "message": "Hello from Backend"
  }
  ```
